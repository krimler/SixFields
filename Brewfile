# Pinned in spirit by versions.env; `make doctor` fails when an installed version
# does not match. Homebrew has no per-formula pinning, so doctor is the gate.
brew "go"
brew "kind"
brew "kubernetes-cli"
brew "clusterctl"
brew "kustomize"
brew "golangci-lint"
brew "yamllint"
brew "kubeconform"
brew "jq"
# envsubst: hack/render.sh and hack/dev-up.sh substitute pinned values after kustomize.
brew "gettext"

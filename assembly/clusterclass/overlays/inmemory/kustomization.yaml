---
# Same class name and same variables as the docker overlay, so a Cluster written
# against one substrate applies unchanged to the other. No nodeImage patch: the
# inMemory backend boots no image.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
- infrastructure.yaml

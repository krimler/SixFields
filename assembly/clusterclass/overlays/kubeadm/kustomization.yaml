---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../backends/docker
- clusterclass.yaml
- control-plane-template.yaml
- bootstrap-template.yaml

# See the docker overlay: installing the assembly is the operator break-glass case.
labels:
- includeSelectors: false
  pairs:
    sixfields.io/break-glass: "true"

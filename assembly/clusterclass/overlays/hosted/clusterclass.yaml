---
# The hosted class. PLAN.md Phase 4 predicted this: upstream allows exactly one
# controlPlane per ClusterClass (api@v1.14.2 core/v1beta2/clusterclass_types.go),
# so placement resolves to a second class rather than to a patch, and
# internal/gen maps placement: hosted to this name.
#
# It differs from std in exactly two references, the control plane and the
# worker bootstrap, and TestAssembly_PlacementsDifferInTwoReferences holds that.
apiVersion: cluster.x-k8s.io/v1beta2
kind: ClusterClass
metadata:
  name: std-hosted
spec:
  infrastructure:
    templateRef:
      apiVersion: infrastructure.cluster.x-k8s.io/v1beta2
      kind: DevClusterTemplate
      name: std-cluster
  controlPlane:
    # No machineInfrastructure: a hosted control plane has no machines at all.
    templateRef:
      apiVersion: controlplane.cluster.x-k8s.io/v1beta1
      kind: K0smotronControlPlaneTemplate
      name: std-hosted-control-plane
  workers:
    machineDeployments:
    - class: default
      bootstrap:
        templateRef:
          apiVersion: bootstrap.cluster.x-k8s.io/v1beta1
          kind: K0sWorkerConfigTemplate
          name: std-default-bootstrap
      infrastructure:
        templateRef:
          apiVersion: infrastructure.cluster.x-k8s.io/v1beta2
          kind: DevMachineTemplate
          name: std-default-machine
  variables:
  # Control-plane replicas cannot be reached by a ClusterClass patch: the
  # topology controller preserves spec.replicas on the control plane object when
  # it applies patched templates
  # (cluster-api@v1.14.2 core/reconcilers/topology/cluster/patches/patch.go:61-65,
  # engine.go:635), and KubeadmControlPlaneTemplate has no replicas field
  # (controlplane/kubeadm/v1beta2/kubeadmcontrolplanetemplate_types.go:89).
  # size is therefore consumed by the generator, which writes
  # Cluster.spec.topology.controlPlane.replicas: dev -> 1, ha -> 3.
  - name: size
    required: true
    schema:
      openAPIV3Schema:
        type: string
        enum: ["dev", "ha"]
        default: dev
        description: "dev is a single control-plane machine, ha is three."
  # A ClusterClass has exactly one controlPlane (clusterclass_types.go:114), so
  # hosted resolves to a second class in Phase 4 rather than to a patch here.
  - name: placement
    required: true
    schema:
      openAPIV3Schema:
        type: string
        enum: ["self", "hosted"]
        default: self
        description: "self runs the control plane on machines, hosted runs it as pods."
